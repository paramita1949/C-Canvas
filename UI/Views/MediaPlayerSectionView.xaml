<UserControl x:Class="ImageColorChanger.UI.Views.MediaPlayerSectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border x:Name="MediaPlayerPanel"
            Background="#2C2C2C"
            BorderBrush="#404040"
            BorderThickness="0,1,0,0"
            Padding="15,10"
            Visibility="Collapsed">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <Button x:Name="BtnMediaPrev"
                        Content="⏮"
                        Width="40"
                        Height="40"
                        ToolTip="上一首"
                        Style="{StaticResource MaterialDesignFloatingActionMiniButton}"/>

                <Button x:Name="BtnMediaPlayPause"
                        Content="▶"
                        Width="50"
                        Height="50"
                        Margin="10,0"
                        ToolTip="播放/暂停"
                        Style="{StaticResource MaterialDesignFloatingActionButton}"/>

                <Button x:Name="BtnMediaNext"
                        Content="⏭"
                        Width="40"
                        Height="40"
                        ToolTip="下一首"
                        Style="{StaticResource MaterialDesignFloatingActionMiniButton}"/>

                <Button x:Name="BtnMediaStop"
                        Content="⏹"
                        Width="40"
                        Height="40"
                        Margin="10,0,0,0"
                        ToolTip="停止"
                        Style="{StaticResource MaterialDesignFloatingActionMiniButton}"/>
            </StackPanel>

            <Grid Grid.Column="1" Margin="20,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Slider x:Name="MediaProgressSlider"
                        Grid.Row="0"
                        VerticalAlignment="Center"
                        Margin="0,0,0,5"
                        Minimum="0"
                        Maximum="100"
                        Value="0"/>

                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock x:Name="MediaCurrentTime" Text="00:00" Foreground="White" FontSize="12"/>
                    <TextBlock Text=" / " Foreground="#999999" FontSize="12" Margin="5,0"/>
                    <TextBlock x:Name="MediaTotalTime" Text="00:00" Foreground="White" FontSize="12"/>
                </StackPanel>
            </Grid>

            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                <Button x:Name="BtnPlayMode"
                        Content="🔀"
                        Width="40"
                        Height="40"
                        ToolTip="播放模式：随机 (默认)"
                        Style="{StaticResource MaterialDesignFloatingActionMiniButton}"/>

                <TextBlock Text="🔊"
                           FontSize="18"
                           VerticalAlignment="Center"
                           Foreground="White"
                           Margin="15,0,5,0"/>

                <Slider x:Name="VolumeSlider"
                        Width="100"
                        VerticalAlignment="Center"
                        Minimum="0"
                        Maximum="100"
                        Value="50"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
