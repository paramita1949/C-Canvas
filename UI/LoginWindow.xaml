<Window x:Class="ImageColorChanger.UI.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="账号登录 - Canvas Cast" 
        Height="550" 
        Width="550"
        MinHeight="500" 
        MinWidth="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F5F5F5"
        FontFamily="Microsoft YaHei UI"
        Icon="/baodian.ico"
        Loaded="Window_Loaded">
    
    <Window.Resources>
        <!-- 按钮样式 -->
        <Style x:Key="TabButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="50"/>
            <Setter Property="MinWidth" Value="150"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Margin" Value="10,0"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                SnapsToDevicePixels="True">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="0" BlurRadius="6" Opacity="0.15" Color="#000000"/>
                            </Border.Effect>
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E3F2FD"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#2196F3"/>
                                <Setter Property="Foreground" Value="#1976D2"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect ShadowDepth="0" BlurRadius="12" Opacity="0.25" Color="#2196F3"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="Selected">
                                <Setter TargetName="border" Property="Background" Value="#2196F3"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#1976D2"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect ShadowDepth="1" BlurRadius="10" Opacity="0.3" Color="#2196F3"/>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#CCCCCC"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 文本框样式 -->
        <Style x:Key="InputBox" TargetType="TextBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style x:Key="PasswordBox" TargetType="PasswordBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Border Grid.Row="0" Background="#2196F3" Padding="20,15">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="2" BlurRadius="8" Opacity="0.2" Color="#000000"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="Canvas Cast" 
                           FontSize="24" 
                           FontWeight="Bold" 
                           Foreground="White"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="感谢您使用 Canvas Cast，登录后解除投影限制" 
                           FontSize="13" 
                           Foreground="#E3F2FD"
                           HorizontalAlignment="Center"
                           Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- 主内容区 -->
        <Grid Grid.Row="1" Background="White">
            <!-- 登录表单（居中显示） -->
            <StackPanel HorizontalAlignment="Center" 
                        VerticalAlignment="Center"
                        Margin="40">
                <TextBlock Text="📱 账号登录" 
                           FontSize="20" 
                           FontWeight="Medium"
                           Foreground="#333333"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,30"/>

                <!-- 用户名 -->
                <TextBlock Text="用户名："
                           FontSize="14"
                           Foreground="#333333"
                           Margin="0,0,0,8"/>
                <TextBox x:Name="UsernameTextBox" 
                         Style="{StaticResource InputBox}"
                         Width="350"
                         TabIndex="1"
                         InputMethod.PreferredImeState="Off"
                         InputMethod.IsInputMethodEnabled="False"
                         Margin="0,0,0,15"/>

                <!-- 密码 -->
                <TextBlock Text="密码："
                           FontSize="14"
                           Foreground="#333333"
                           Margin="0,0,0,8"/>
                <PasswordBox x:Name="PasswordBox" 
                             Style="{StaticResource PasswordBox}"
                             Width="350"
                             TabIndex="2"
                             KeyDown="PasswordBox_KeyDown"
                             Margin="0,0,0,20"/>

                <!-- 状态信息 -->
                <TextBlock x:Name="StatusText" 
                           TextWrapping="Wrap"
                           FontSize="12"
                           Foreground="Red"
                           HorizontalAlignment="Center"
                           Visibility="Collapsed"
                           Margin="0,0,0,15"/>

                <!-- 登录按钮 -->
                <Button x:Name="LoginButton"
                        Content="登录"
                        Style="{StaticResource TabButtonStyle}"
                        Width="350"
                        Margin="0,0,0,20"
                        Click="LoginButton_Click"
                        TabIndex="3"/>

                <!-- 注册和付费按钮 -->
                <StackPanel Orientation="Horizontal" 
                            HorizontalAlignment="Center"
                            Margin="0,0,0,0">
                    <Button x:Name="RegisterButton"
                            Content="注册账号"
                            Style="{StaticResource TabButtonStyle}"
                            Width="170"
                            Height="40"
                            FontSize="14"
                            Margin="0,0,5,0"
                            Click="RegisterButton_Click"
                            TabIndex="4"/>
                    <Button x:Name="PaymentButton"
                            Content="💰 付费购买"
                            Style="{StaticResource TabButtonStyle}"
                            Width="170"
                            Height="40"
                            FontSize="14"
                            Margin="5,0,0,0"
                            Click="PaymentButton_Click"
                            TabIndex="5"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
</Window>

