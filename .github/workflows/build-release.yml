name: C#/WPF构建和发布

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  PROJECT_NAME: ImageColorChanger
  OUTPUT_NAME: Canvas Cast
  DOTNET_VERSION: '8.0.x'

permissions:
  contents: write

jobs:
  build:
    name: 编译和打包
    runs-on: windows-latest
    
    steps:
    - name: 检出代码
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
    
    - name: 设置 .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}
    
    - name: 还原依赖
      run: dotnet restore
    
    - name: 编译项目
      run: dotnet build ImageColorChanger.csproj --configuration Release --no-restore
    
    - name: 发布项目
      run: dotnet publish ImageColorChanger.csproj -c Release -o publish --self-contained false
    
    - name: 获取版本号
      id: get_version
      shell: pwsh
      run: |
        if ($env:GITHUB_REF -like 'refs/tags/*') {
          $tag = $env:GITHUB_REF -replace 'refs/tags/', ''
          $version = $tag
          echo "检测到推送 tag: $tag"
        } else {
          $runNumber = $env:GITHUB_RUN_NUMBER
          $tagBase = "auto-$runNumber"

          try {
            $shortSha = (git rev-parse --short HEAD).Trim()
          } catch {
            $shortSha = ''
            echo "警告: 无法获取提交哈希，tag 将不包含哈希"
          }

          if ($shortSha) {
            $tag = "$tagBase-$shortSha"
          } else {
            $tag = $tagBase
          }

          $version = $tag
          echo "未检测到外部 tag，自动生成 tag: $tag"
        }

        echo "version=$version" >> $env:GITHUB_OUTPUT
        echo "tag=$tag" >> $env:GITHUB_OUTPUT
        echo "构建版本: $version"
        echo "标签: $tag"
    
    - name: 验证构建产物
      shell: pwsh
      run: |
        echo "=== 验证构建产物 ==="
        
        # 检查主程序
        if (Test-Path "publish\Canvas Cast.exe") {
          echo "✅ 主程序验证成功"
          $exeInfo = Get-Item "publish\Canvas Cast.exe"
          echo "主程序大小: $([math]::Round($exeInfo.Length / 1MB, 2)) MB"
        } else {
          echo "❌ 构建产物验证失败 - Canvas Cast.exe 不存在"
          echo "publish目录内容:"
          Get-ChildItem "publish" -ErrorAction SilentlyContinue | ForEach-Object { echo "  - $($_.Name)" }
          exit 1
        }
        
        # 检查并处理PAK文件
        if (Test-Path "publish\Resources.pak") {
          echo "✅ PAK文件已存在于publish目录"
          $pakInfo = Get-Item "publish\Resources.pak"
          echo "PAK文件大小: $([math]::Round($pakInfo.Length / 1MB, 2)) MB"
        } else {
          echo "⚠️ PAK文件不存在于publish目录，尝试从bin目录复制..."
          if (Test-Path "bin\Release\net8.0-windows\Resources.pak") {
            Copy-Item "bin\Release\net8.0-windows\Resources.pak" "publish\Resources.pak"
            echo "✅ PAK文件已从bin目录复制到publish根目录"
            $pakInfo = Get-Item "publish\Resources.pak"
            echo "PAK文件大小: $([math]::Round($pakInfo.Length / 1MB, 2)) MB"
          } else {
            echo "❌ 错误: bin目录中也没有PAK文件"
            exit 1
          }
        }
        
        # 删除publish目录中的bin文件夹（如果存在）
        if (Test-Path "publish\bin") {
          echo "⚠️ 发现publish\bin目录，正在删除..."
          Remove-Item "publish\bin" -Recurse -Force
          echo "✅ 已删除publish\bin目录"
        }
        
        echo ""
        echo "=== publish目录最终内容 ==="
        Get-ChildItem "publish" -Name | ForEach-Object { echo "  - $_" }
    
    - name: 创建发布包
      shell: pwsh
      run: |
        $version = "${{ steps.get_version.outputs.version }}"
        $tag = "${{ steps.get_version.outputs.tag }}"
        $fullZipName = "Canvas-Full.zip"
        $updateZipName = "Canvas-Update.zip"
        
        # ========== 创建完整包 ==========
        echo "=== 创建完整包 ==="
        Compress-Archive -Path publish\* -DestinationPath $fullZipName
        
        # 验证完整包
        if (Test-Path $fullZipName) {
          $size = (Get-Item $fullZipName).Length / 1MB
          echo "✅ 完整包创建成功"
          echo "包名称: $fullZipName"
          echo "包大小: $([math]::Round($size, 2)) MB"
        } else {
          echo "❌ 完整包创建失败"
          exit 1
        }
        
        # ========== 创建增量更新包 ==========
        echo ""
        echo "=== 创建增量更新包 ==="
        
        # 创建临时目录存放更新文件
        $updateDir = "update_temp"
        New-Item -ItemType Directory -Path $updateDir -Force | Out-Null
        
        # 复制核心文件到临时目录
        $dllFile = "publish\Canvas Cast.dll"
        $pakFile = "publish\Resources.pak"
        
        if (Test-Path $dllFile) {
          Copy-Item $dllFile $updateDir\
          $dllSize = (Get-Item $dllFile).Length / 1MB
          echo "✅ 已添加 Canvas Cast.dll ($([math]::Round($dllSize, 2)) MB)"
        } else {
          echo "❌ 错误: Canvas Cast.dll 不存在"
          exit 1
        }
        
        if (Test-Path $pakFile) {
          Copy-Item $pakFile $updateDir\
          $pakSize = (Get-Item $pakFile).Length / 1MB
          echo "✅ 已添加 Resources.pak ($([math]::Round($pakSize, 2)) MB)"
        } else {
          echo "⚠️ 警告: Resources.pak 不存在，继续..."
        }
        
        # 压缩更新包
        Compress-Archive -Path $updateDir\* -DestinationPath $updateZipName
        
        # 验证更新包
        if (Test-Path $updateZipName) {
          $updateSize = (Get-Item $updateZipName).Length / 1MB
          echo "✅ 增量更新包创建成功"
          echo "包名称: $updateZipName"
          echo "包大小: $([math]::Round($updateSize, 2)) MB"
        } else {
          echo "❌ 增量更新包创建失败"
          exit 1
        }
        
        # 清理临时目录
        Remove-Item $updateDir -Recurse -Force
        
        # ========== 输出统计信息 ==========
        echo ""
        echo "=== 打包完成统计 ==="
        echo "完整包: $fullZipName - $([math]::Round((Get-Item $fullZipName).Length / 1MB, 2)) MB"
        echo "更新包: $updateZipName - $([math]::Round((Get-Item $updateZipName).Length / 1MB, 2)) MB"
        $ratio = ((Get-Item $updateZipName).Length / (Get-Item $fullZipName).Length) * 100
        echo "更新包仅为完整包的 $([math]::Round($ratio, 1))%"
        
        # 输出文件名供后续步骤使用
        echo "full_zip_name=$fullZipName" >> $env:GITHUB_OUTPUT
        echo "update_zip_name=$updateZipName" >> $env:GITHUB_OUTPUT
      id: create_package
    
    - name: 上传完整包
      uses: actions/upload-artifact@v4
      with:
        name: Canvas-Cast-Full-${{ steps.get_version.outputs.version }}
        path: ${{ steps.create_package.outputs.full_zip_name }}
        retention-days: 30
    
    - name: 上传更新包
      uses: actions/upload-artifact@v4
      with:
        name: Canvas-Cast-Update-${{ steps.get_version.outputs.version }}
        path: ${{ steps.create_package.outputs.update_zip_name }}
        retention-days: 30
    
    - name: 调试Release信息
      shell: pwsh
      run: |
        echo "=== Release调试信息 ==="
        echo "版本号: ${{ steps.get_version.outputs.version }}"
        echo "标签: ${{ steps.get_version.outputs.tag }}"
        echo ""
        echo "完整包: ${{ steps.create_package.outputs.full_zip_name }}"
        echo "完整包存在: $(Test-Path '${{ steps.create_package.outputs.full_zip_name }}')"
        if (Test-Path '${{ steps.create_package.outputs.full_zip_name }}') {
          $size = (Get-Item '${{ steps.create_package.outputs.full_zip_name }}').Length / 1MB
          echo "完整包大小: $([math]::Round($size, 2)) MB"
        }
        echo ""
        echo "更新包: ${{ steps.create_package.outputs.update_zip_name }}"
        echo "更新包存在: $(Test-Path '${{ steps.create_package.outputs.update_zip_name }}')"
        if (Test-Path '${{ steps.create_package.outputs.update_zip_name }}') {
          $size = (Get-Item '${{ steps.create_package.outputs.update_zip_name }}').Length / 1MB
          echo "更新包大小: $([math]::Round($size, 2)) MB"
        }
        echo "======================"
    
    - name: 检查并删除已存在的 Release
      shell: pwsh
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        $tag = "${{ steps.get_version.outputs.tag }}"
        echo "=== 检查 Release 是否存在 ==="
        echo "标签: $tag"
        
        # 获取 Release 信息
        $headers = @{
          "Authorization" = "Bearer $env:GITHUB_TOKEN"
          "Accept" = "application/vnd.github+json"
          "X-GitHub-Api-Version" = "2022-11-28"
        }
        
        $repo = "${{ github.repository }}"
        $releaseUrl = "https://api.github.com/repos/$repo/releases/tags/$tag"
        
        try {
          $response = Invoke-WebRequest -Uri $releaseUrl -Headers $headers -Method Get -ErrorAction Stop
          $releaseData = $response.Content | ConvertFrom-Json
          $releaseId = $releaseData.id
          
          echo "⚠️ 发现已存在的 Release (ID: $releaseId)，正在删除..."
          
          # 删除 Release
          $deleteUrl = "https://api.github.com/repos/$repo/releases/$releaseId"
          Invoke-WebRequest -Uri $deleteUrl -Headers $headers -Method Delete -ErrorAction Stop | Out-Null
          echo "✅ 已删除旧的 Release"
          
          # 等待一下确保删除完成
          Start-Sleep -Seconds 2
        } catch {
          if ($_.Exception.Response.StatusCode -eq 404) {
            echo "✅ Release 不存在，无需删除"
          } else {
            echo "⚠️ 检查 Release 时出错: $($_.Exception.Message)"
          }
        }
        
        # 检查并删除 Tag
        $tagUrl = "https://api.github.com/repos/$repo/git/refs/tags/$tag"
        try {
          Invoke-WebRequest -Uri $tagUrl -Headers $headers -Method Get -ErrorAction Stop | Out-Null
          echo "⚠️ 发现已存在的 Tag，正在删除..."
          
          Invoke-WebRequest -Uri $tagUrl -Headers $headers -Method Delete -ErrorAction Stop | Out-Null
          echo "✅ 已删除旧的 Tag"
          
          # 等待一下确保删除完成
          Start-Sleep -Seconds 2
        } catch {
          if ($_.Exception.Response.StatusCode -eq 404) {
            echo "✅ Tag 不存在，无需删除"
          } else {
            echo "⚠️ 检查 Tag 时出错: $($_.Exception.Message)"
          }
        }
        
        echo "=== 检查完成，准备创建新的 Release ==="
    
    - name: 创建 GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        files: |
          ${{ steps.create_package.outputs.full_zip_name }}
          ${{ steps.create_package.outputs.update_zip_name }}
        tag_name: ${{ steps.get_version.outputs.tag }}
        name: Canvas Cast ${{ steps.get_version.outputs.tag }}
        draft: false
        prerelease: false
        generate_release_notes: true
        body: |
          ## 🎉 Canvas Cast ${{ steps.get_version.outputs.tag }}
          
          ### 📦 下载说明
          
          本次发布提供两个安装包，请根据需要选择：
          
          #### 🆕 新用户 - 完整安装包
          - 📥 下载 `Canvas-Full.zip` 
          - 📂 解压到任意目录
          - ▶️ 运行 `Canvas Cast.exe`
          
          #### 🔄 老用户 - 增量更新包（推荐）
          - 📥 下载 `Canvas-Update.zip` （体积更小，下载更快）
          - 📂 解压得到 `Canvas Cast.dll` 和 `Resources.pak`
          - 🔁 覆盖原程序目录中的同名文件
          - ▶️ 重启程序即可
          
          > **提示**: 增量更新包仅包含核心程序文件，体积小，适合快速更新
          
          ### ⚙️ 系统要求
          - Windows 10 或更高版本
          - .NET 8.0 Runtime (如未安装会自动提示下载)
          
          ### 📝 更新日志
          请查看下方自动生成的变更记录
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    
    - name: 构建摘要
      shell: pwsh
      run: |
        echo "### ✅ 构建成功" >> $env:GITHUB_STEP_SUMMARY
        echo "" >> $env:GITHUB_STEP_SUMMARY
        echo "- **版本**: ${{ steps.get_version.outputs.version }}" >> $env:GITHUB_STEP_SUMMARY
        echo "- **平台**: Windows x64" >> $env:GITHUB_STEP_SUMMARY
        echo "- **.NET**: ${{ env.DOTNET_VERSION }}" >> $env:GITHUB_STEP_SUMMARY
        echo "" >> $env:GITHUB_STEP_SUMMARY
        echo "### 📦 发布包" >> $env:GITHUB_STEP_SUMMARY
        echo "" >> $env:GITHUB_STEP_SUMMARY
        
        $fullSize = [math]::Round((Get-Item "${{ steps.create_package.outputs.full_zip_name }}").Length / 1MB, 2)
        $updateSize = [math]::Round((Get-Item "${{ steps.create_package.outputs.update_zip_name }}").Length / 1MB, 2)
        $ratio = [math]::Round(($updateSize / $fullSize) * 100, 1)
        
        echo "- **完整包**: ``${{ steps.create_package.outputs.full_zip_name }}`` - $fullSize MB" >> $env:GITHUB_STEP_SUMMARY
        echo "- **更新包**: ``${{ steps.create_package.outputs.update_zip_name }}`` - $updateSize MB" >> $env:GITHUB_STEP_SUMMARY
        echo "- **节省比例**: 更新包仅为完整包的 $ratio%" >> $env:GITHUB_STEP_SUMMARY

