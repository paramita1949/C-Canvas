name: 构建和发布（混淆保护）

on:
  push:
    branches: [ main, master ]
    paths:
      - '**.cs'
      - '**.xaml'
      - '**.csproj'
      - 'updata.txt'
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

env:
  PROJECT_NAME: ImageColorChanger
  OUTPUT_NAME: Canvas Cast
  DOTNET_VERSION: '8.0.x'

permissions:
  contents: write

jobs:
  build:
    name: 编译、混淆和发布
    runs-on: windows-latest
    
    steps:
    - name: 📥 检出代码
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
    
    - name: 🔧 设置 .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}
    
    - name: 📦 还原依赖
      run: dotnet restore ${{ env.PROJECT_NAME }}.csproj
    
    - name: 🔨 编译 Release 版本
      run: dotnet build ${{ env.PROJECT_NAME }}.csproj -c Release --no-restore
    
    - name: 📦 发布调试版（用于 Release）
      run: dotnet publish ${{ env.PROJECT_NAME }}.csproj -c Release -o publish-original --self-contained false
    
    - name: 🔧 处理调试版 PAK 文件
      shell: pwsh
      run: |
        Write-Host "=== 处理调试版 PAK 文件 ===" -ForegroundColor Cyan
        
        # 检查 publish-original 根目录是否有 PAK 文件
        if (Test-Path "publish-original\Resources.pak") {
          $pakSize = (Get-Item "publish-original\Resources.pak").Length / 1MB
          Write-Host "✅ PAK 文件已在根目录: $([math]::Round($pakSize, 2)) MB" -ForegroundColor Green
        } else {
          Write-Host "⚠️ PAK 文件不在根目录，尝试查找..." -ForegroundColor Yellow
          
          # 在 bin 目录中查找
          if (Test-Path "publish-original\bin\Resources.pak") {
            Write-Host "  找到 PAK 文件在 bin 目录中，复制到根目录..." -ForegroundColor Yellow
            Copy-Item "publish-original\bin\Resources.pak" "publish-original\Resources.pak"
            $pakSize = (Get-Item "publish-original\Resources.pak").Length / 1MB
            Write-Host "✅ PAK 文件已复制到根目录: $([math]::Round($pakSize, 2)) MB" -ForegroundColor Green
          } elseif (Test-Path "bin\Release\net8.0-windows\Resources.pak") {
            Write-Host "  从编译目录复制 PAK 文件..." -ForegroundColor Yellow
            Copy-Item "bin\Release\net8.0-windows\Resources.pak" "publish-original\Resources.pak"
            $pakSize = (Get-Item "publish-original\Resources.pak").Length / 1MB
            Write-Host "✅ PAK 文件已复制到根目录: $([math]::Round($pakSize, 2)) MB" -ForegroundColor Green
          } else {
            Write-Host "❌ 错误: 未找到 PAK 文件！" -ForegroundColor Red
            Write-Host "查找位置:" -ForegroundColor Yellow
            Write-Host "  - publish-original\Resources.pak" -ForegroundColor Gray
            Write-Host "  - publish-original\bin\Resources.pak" -ForegroundColor Gray
            Write-Host "  - bin\Release\net8.0-windows\Resources.pak" -ForegroundColor Gray
            exit 1
          }
        }
        
        Write-Host ""
        Write-Host "=== publish-original 目录统计 ===" -ForegroundColor Cyan
        $fileCount = (Get-ChildItem "publish-original" -File | Measure-Object).Count
        $dirCount = (Get-ChildItem "publish-original" -Directory | Measure-Object).Count
        Write-Host "文件数量: $fileCount" -ForegroundColor Green
        Write-Host "目录数量: $dirCount" -ForegroundColor Green
        
        Write-Host ""
        Write-Host "根目录主要文件:" -ForegroundColor Yellow
        Get-ChildItem "publish-original" -File | Where-Object { $_.Extension -in ".exe",".dll",".pak",".json" } | ForEach-Object {
          Write-Host "  - $($_.Name)" -ForegroundColor Gray
        }
    
    - name: 📋 获取版本号
      id: get_version
      shell: pwsh
      run: |
        # 从 MainWindow.xaml 读取版本号
        $xamlFile = "UI\MainWindow.xaml"
        if (Test-Path $xamlFile) {
          $content = Get-Content $xamlFile -Raw
          if ($content -match 'Title="[^"]*V(\d+\.\d+\.\d+)"') {
            $version = $matches[1]
            $tag = "V$version"
            echo "version=$version" >> $env:GITHUB_OUTPUT
            echo "tag=$tag" >> $env:GITHUB_OUTPUT
            Write-Host "读取到版本号: $version"
            Write-Host "生成标签: $tag"
          } else {
            $version = "1.0.0"
            $tag = "V$version"
            echo "version=$version" >> $env:GITHUB_OUTPUT
            echo "tag=$tag" >> $env:GITHUB_OUTPUT
            Write-Host "未找到版本号，使用默认: $version"
          }
        }
    
    - name: 📦 显示编译信息
      shell: pwsh
      run: |
        Write-Host "=================================" -ForegroundColor Cyan
        Write-Host "📦 编译信息" -ForegroundColor Cyan
        Write-Host "=================================" -ForegroundColor Cyan
        Write-Host "项目: ${{ env.PROJECT_NAME }}"
        Write-Host "版本: ${{ steps.get_version.outputs.version }}"
        Write-Host ""
        
        $exePath = "bin\Release\net8.0-windows\${{ env.PROJECT_NAME }}.exe"
        if (Test-Path $exePath) {
          $fileSize = (Get-Item $exePath).Length / 1MB
          Write-Host "原始文件大小: $([math]::Round($fileSize, 2)) MB" -ForegroundColor Green
        }
    
    - name: 🔒 安装 Obfuscar
      shell: pwsh
      run: |
        Write-Host "安装 Obfuscar 混淆工具（支持 .NET 8）..." -ForegroundColor Yellow
        dotnet tool install --global Obfuscar.GlobalTool
        Write-Host "✅ Obfuscar 安装成功" -ForegroundColor Green
    
    - name: 🔐 混淆代码
      shell: pwsh
      run: |
        Write-Host "=================================" -ForegroundColor Cyan
        Write-Host "🔐 开始混淆代码" -ForegroundColor Cyan
        Write-Host "=================================" -ForegroundColor Cyan
        
        # Add .NET tools to PATH
        $env:PATH = "$env:USERPROFILE\.dotnet\tools;$env:PATH"
        
        # Change to the build output directory
        cd "bin\Release\net8.0-windows"
        
        # Debug: List files
        Write-Host "Files in build directory:" -ForegroundColor Yellow
        Get-ChildItem -Name
        
        # Check if exe exists
        $exeName = "Canvas Cast.exe"
        if (Test-Path $exeName) {
          $fileInfo = Get-Item $exeName
          Write-Host "Found: $exeName - Size: $($fileInfo.Length) bytes" -ForegroundColor Green
          
          # Check if it's a valid .NET assembly
          try {
            $assembly = [System.Reflection.Assembly]::LoadFrom((Resolve-Path $exeName).Path)
            Write-Host "Assembly loaded successfully: $($assembly.FullName)" -ForegroundColor Green
          } catch {
            Write-Host "WARNING: Failed to load as .NET assembly: $_" -ForegroundColor Red
          }
        } else {
          Write-Host "ERROR: $exeName not found!" -ForegroundColor Red
          exit 1
        }
        
        # Copy obfuscar config file to build directory
        Copy-Item "..\..\..\obfuscar.xml" -Destination "."
        
        # Run obfuscation with Obfuscar (only DLL, not EXE)
        obfuscar.console obfuscar.xml
        
        if ($LASTEXITCODE -ne 0) {
          Write-Host "❌ 混淆失败！" -ForegroundColor Red
          exit 1
        }
        
        Write-Host "✅ 混淆完成！" -ForegroundColor Green
        
        # Verify obfuscated DLL
        $obfuscatedDll = "Confused\${{ env.OUTPUT_NAME }}.dll"
        if (Test-Path $obfuscatedDll) {
          $dllSize = (Get-Item $obfuscatedDll).Length / 1MB
          Write-Host "混淆后 DLL 大小: $([math]::Round($dllSize, 2)) MB" -ForegroundColor Green
        } else {
          Write-Host "❌ 未找到混淆后的 DLL！" -ForegroundColor Red
          exit 1
        }
        
        # Copy EXE and other files to Confused directory
        Write-Host "复制启动器和依赖文件..." -ForegroundColor Yellow
        Write-Host ""
        
        # 先复制所有文件（除了主 DLL）到 Confused 目录
        Get-ChildItem -File | Where-Object { $_.Name -ne "${{ env.OUTPUT_NAME }}.dll" } | ForEach-Object {
          Copy-Item $_.FullName -Destination "Confused\" -Force
          Write-Host "  复制文件: $($_.Name)" -ForegroundColor Gray
        }
        
        # 复制所有子目录（runtimes、libvlc 等）
        Get-ChildItem -Directory | Where-Object { $_.Name -ne "Confused" } | ForEach-Object {
          Copy-Item $_.FullName -Destination "Confused\$($_.Name)" -Recurse -Force
          Write-Host "  复制目录: $($_.Name)" -ForegroundColor Gray
        }
        
        # 列出 Confused 目录内容以便检查
        Write-Host ""
        Write-Host "=== Confused 目录内容 ===" -ForegroundColor Cyan
        $confusedFiles = Get-ChildItem "Confused" -Recurse -File | Measure-Object
        $confusedDirs = Get-ChildItem "Confused" -Recurse -Directory | Measure-Object
        Write-Host "文件数量: $($confusedFiles.Count)" -ForegroundColor Green
        Write-Host "目录数量: $($confusedDirs.Count)" -ForegroundColor Green
        Write-Host ""
        Write-Host "根目录文件:" -ForegroundColor Yellow
        Get-ChildItem "Confused" -File -Name | ForEach-Object { Write-Host "  - $_" }
        Write-Host ""
        Write-Host "子目录:" -ForegroundColor Yellow
        Get-ChildItem "Confused" -Directory -Name | ForEach-Object { Write-Host "  - $_" }
        
        Write-Host ""
        Write-Host "✅ 所有文件已复制到 Confused 目录" -ForegroundColor Green
    
    - name: 📦 打包发布文件
      shell: pwsh
      run: |
        $publishDir = "publish"
        $confusedDir = "bin\Release\net8.0-windows\Confused"
        
        # 创建发布目录
        New-Item -ItemType Directory -Force -Path $publishDir | Out-Null
        
        # 打包正式版文件
        Write-Host "📦 打包正式版文件..." -ForegroundColor Green
        Copy-Item "$confusedDir\*" -Destination $publishDir -Recurse -Force
        
        # 删除可能复制进来的 bin 文件夹
        if (Test-Path "$publishDir\bin") {
          Write-Host "⚠️ 发现并删除 bin 文件夹..." -ForegroundColor Yellow
          Remove-Item "$publishDir\bin" -Recurse -Force
        }
        
        Write-Host "✅ 打包完成" -ForegroundColor Green
    
    - name: ✅ 验证构建产物
      shell: pwsh
      run: |
        Write-Host "=== 验证构建产物 ===" -ForegroundColor Cyan
        
        # 检查主程序
        if (Test-Path "publish\${{ env.OUTPUT_NAME }}.exe") {
          Write-Host "✅ 主程序验证成功" -ForegroundColor Green
          $exeInfo = Get-Item "publish\${{ env.OUTPUT_NAME }}.exe"
          Write-Host "主程序大小: $([math]::Round($exeInfo.Length / 1MB, 2)) MB"
        } else {
          Write-Host "❌ 构建产物验证失败 - ${{ env.OUTPUT_NAME }}.exe 不存在" -ForegroundColor Red
          Write-Host "publish目录内容:" -ForegroundColor Yellow
          Get-ChildItem "publish" -ErrorAction SilentlyContinue | ForEach-Object { Write-Host "  - $($_.Name)" }
          exit 1
        }
        
        # 检查并处理PAK文件
        if (Test-Path "publish\Resources.pak") {
          Write-Host "✅ PAK文件已存在于publish目录" -ForegroundColor Green
          $pakInfo = Get-Item "publish\Resources.pak"
          Write-Host "PAK文件大小: $([math]::Round($pakInfo.Length / 1MB, 2)) MB"
        } else {
          Write-Host "⚠️ PAK文件不存在于publish目录，尝试从Confused目录复制..." -ForegroundColor Yellow
          if (Test-Path "bin\Release\net8.0-windows\Confused\Resources.pak") {
            Copy-Item "bin\Release\net8.0-windows\Confused\Resources.pak" "publish\Resources.pak"
            Write-Host "✅ PAK文件已从Confused目录复制到publish根目录" -ForegroundColor Green
            $pakInfo = Get-Item "publish\Resources.pak"
            Write-Host "PAK文件大小: $([math]::Round($pakInfo.Length / 1MB, 2)) MB"
          } elseif (Test-Path "bin\Release\net8.0-windows\Resources.pak") {
            Copy-Item "bin\Release\net8.0-windows\Resources.pak" "publish\Resources.pak"
            Write-Host "✅ PAK文件已从bin目录复制到publish根目录" -ForegroundColor Green
            $pakInfo = Get-Item "publish\Resources.pak"
            Write-Host "PAK文件大小: $([math]::Round($pakInfo.Length / 1MB, 2)) MB"
          } else {
            Write-Host "❌ 错误: 未找到PAK文件" -ForegroundColor Red
            exit 1
          }
        }
        
        # 确认没有 bin 文件夹（应该在打包步骤已删除）
        if (Test-Path "publish\bin") {
          Write-Host "❌ 错误: publish\bin目录不应该存在！" -ForegroundColor Red
          exit 1
        }
        
        Write-Host ""
        Write-Host "=== publish目录最终内容 ===" -ForegroundColor Cyan
        Get-ChildItem "publish" -Name | ForEach-Object { Write-Host "  - $_" }
    
    - name: 创建发布包
      id: create_package
      shell: pwsh
      run: |
        $version = "${{ steps.get_version.outputs.version }}"
        $tag = "${{ steps.get_version.outputs.tag }}"
        $releaseZip = "Canvas-Cast-$tag-win-x64.zip"
        $debugZip = "Canvas-Cast-$tag-win-x64-Debug.zip"
        
        # 压缩正式版（应该已经没有 bin 文件夹了）
        Write-Host "创建正式版发布包..." -ForegroundColor Yellow
        
        # 检查是否有 bin 文件夹
        if (Test-Path "publish\bin") {
          Write-Host "⚠️ 警告: publish 目录中仍存在 bin 文件夹，将排除它" -ForegroundColor Yellow
          
          # 创建临时目录
          $tempReleaseDir = "publish-temp"
          New-Item -ItemType Directory -Force -Path $tempReleaseDir | Out-Null
          
          # 复制所有文件和目录，但排除 bin
          Get-ChildItem "publish" | Where-Object { $_.Name -ne "bin" } | ForEach-Object {
            Copy-Item $_.FullName -Destination $tempReleaseDir -Recurse -Force
          }
          
          Compress-Archive -Path "$tempReleaseDir\*" -DestinationPath $releaseZip
          Remove-Item $tempReleaseDir -Recurse -Force
        } else {
          Compress-Archive -Path publish\* -DestinationPath $releaseZip
        }
        
        if (Test-Path $releaseZip) {
          $size = (Get-Item $releaseZip).Length / 1MB
          Write-Host "✅ 正式版发布包创建成功: $([math]::Round($size, 2)) MB" -ForegroundColor Green
        } else {
          Write-Host "❌ 正式版发布包创建失败" -ForegroundColor Red
          exit 1
        }
        
        # 压缩调试版（使用 publish-original 目录，排除 bin 文件夹）
        Write-Host "创建调试版压缩包（排除 bin 文件夹）..." -ForegroundColor Yellow
        
        # 创建临时目录用于压缩
        $tempDir = "publish-original-temp"
        New-Item -ItemType Directory -Force -Path $tempDir | Out-Null
        
        # 复制所有文件和目录，但排除 bin 文件夹
        Get-ChildItem "publish-original" | Where-Object { $_.Name -ne "bin" } | ForEach-Object {
          Copy-Item $_.FullName -Destination $tempDir -Recurse -Force
          Write-Host "  打包: $($_.Name)" -ForegroundColor Gray
        }
        
        # 压缩临时目录
        Compress-Archive -Path "$tempDir\*" -DestinationPath $debugZip
        
        # 删除临时目录
        Remove-Item $tempDir -Recurse -Force
        
        if (Test-Path $debugZip) {
          $size = (Get-Item $debugZip).Length / 1MB
          Write-Host "✅ 调试版发布包创建成功: $([math]::Round($size, 2)) MB" -ForegroundColor Green
        } else {
          Write-Host "❌ 调试版发布包创建失败" -ForegroundColor Red
          exit 1
        }
        
        # 输出文件名供后续步骤使用
        echo "release_zip=$releaseZip" >> $env:GITHUB_OUTPUT
        echo "debug_zip=$debugZip" >> $env:GITHUB_OUTPUT
    
    - name: 📤 上传正式版程序（Artifact）
      uses: actions/upload-artifact@v4
      with:
        name: ${{ env.OUTPUT_NAME }}-v${{ steps.get_version.outputs.version }}
        path: publish/
        retention-days: 30
    
    - name: 📤 上传调试版程序（调试用，7天后删除）
      uses: actions/upload-artifact@v4
      with:
        name: ${{ env.OUTPUT_NAME }}-v${{ steps.get_version.outputs.version }}-Debug
        path: publish-original/
        retention-days: 7
    
    - name: 📊 调试Release信息
      shell: pwsh
      run: |
        Write-Host "=== Release调试信息 ===" -ForegroundColor Cyan
        Write-Host "版本号: ${{ steps.get_version.outputs.version }}"
        Write-Host "标签: ${{ steps.get_version.outputs.tag }}"
        Write-Host "正式版包文件: ${{ steps.create_package.outputs.release_zip }}"
        Write-Host "调试版包文件: ${{ steps.create_package.outputs.debug_zip }}"
        Write-Host "正式版包存在: $(Test-Path '${{ steps.create_package.outputs.release_zip }}')"
        Write-Host "调试版包存在: $(Test-Path '${{ steps.create_package.outputs.debug_zip }}')"
        if (Test-Path '${{ steps.create_package.outputs.release_zip }}') {
          $size = (Get-Item '${{ steps.create_package.outputs.release_zip }}').Length / 1MB
          Write-Host "正式版包大小: $([math]::Round($size, 2)) MB" -ForegroundColor Green
        }
        if (Test-Path '${{ steps.create_package.outputs.debug_zip }}') {
          $size = (Get-Item '${{ steps.create_package.outputs.debug_zip }}').Length / 1MB
          Write-Host "调试版包大小: $([math]::Round($size, 2)) MB" -ForegroundColor Green
        }
        Write-Host "======================" -ForegroundColor Cyan
    
    - name: 🎉 创建 GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        files: |
          ${{ steps.create_package.outputs.release_zip }}
          ${{ steps.create_package.outputs.debug_zip }}
        tag_name: ${{ steps.get_version.outputs.tag }}
        name: Canvas Cast ${{ steps.get_version.outputs.tag }}
        draft: false
        prerelease: false
        generate_release_notes: true
        body: |
          ## 🎉 Canvas Cast ${{ steps.get_version.outputs.tag }}
          
          > 💡 **版本说明**：包含正式版和调试版两个下载选项
          
          ### 📦 下载说明
          - **正式版** (推荐)：`${{ steps.create_package.outputs.release_zip }}` - 正式发布版本
          - **调试版**：`${{ steps.create_package.outputs.debug_zip }}` - 开发调试用版本
          
          ### 📖 使用方法
          1. 下载上述 ZIP 文件
          2. 解压到任意目录
          3. 运行 `Canvas Cast.exe`
          
          ### ⚙️ 系统要求
          - Windows 10 或更高版本
          - .NET 8.0 Runtime (如未安装会自动提示下载)
          
          ### 🔧 技术栈
          - 开发语言：C#
          - UI框架：WPF
          - 运行时：.NET 8.0
          
          ### 📝 更新日志
          请查看下方自动生成的变更记录
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    
    - name: 📊 构建摘要
      shell: pwsh
      run: |
        Write-Host "### ✅ 构建成功" >> $env:GITHUB_STEP_SUMMARY
        Write-Host "" >> $env:GITHUB_STEP_SUMMARY
        Write-Host "- **版本**: ${{ steps.get_version.outputs.version }}" >> $env:GITHUB_STEP_SUMMARY
        Write-Host "- **平台**: Windows x64" >> $env:GITHUB_STEP_SUMMARY
        Write-Host "- **正式版**: ${{ steps.create_package.outputs.release_zip }}" >> $env:GITHUB_STEP_SUMMARY
        Write-Host "- **调试版**: ${{ steps.create_package.outputs.debug_zip }}" >> $env:GITHUB_STEP_SUMMARY
        Write-Host "- **.NET**: ${{ env.DOTNET_VERSION }}" >> $env:GITHUB_STEP_SUMMARY

