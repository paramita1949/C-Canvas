# Canvas Cast - 图片九宫格分割功能设计文档

## 📋 文档信息
- **功能名称**: 图片九宫格分割显示
- **版本**: V1.0
- **创建日期**: 2025-10-19
- **状态**: 设计阶段

---

## 🎯 功能概述

### 核心需求
实现图片显示区域的灵活分割，支持单区域、双区域（横/竖）、四宫格等多种布局模式，每个区域可以独立显示不同的图片。

### 适用场景
1. 多图对比展示（如：原图 vs 处理后）
2. 教学演示（多角度/多步骤同时展示）
3. 拼图式内容展示
4. 多素材组合演示

---

## 🎨 功能设计

### 1. 分割模式定义

```csharp
/// <summary>
/// 图片分割模式
/// </summary>
public enum SplitMode
{
    /// <summary>单区域 - 默认模式，一张图片占满整个显示区域</summary>
    Single = 0,
    
    /// <summary>竖分割 - 左右两块（50% | 50%）</summary>
    VerticalSplit = 1,
    
    /// <summary>横分割 - 上下两块（上50% / 下50%）</summary>
    HorizontalSplit = 2,
    
    /// <summary>四宫格 - 2x2网格布局</summary>
    Grid2x2 = 3,
    
    // 预留扩展：
    // Grid3x3 = 4,     // 3x3九宫格
    // Custom = 99       // 自定义布局
}
```

### 2. 区域配置模型

```csharp
/// <summary>
/// 单个分割区域的配置
/// </summary>
public class SplitRegion
{
    /// <summary>区域ID（0-3）</summary>
    public int RegionId { get; set; }
    
    /// <summary>区域相对位置（百分比）</summary>
    public RectangleF Bounds { get; set; } // X, Y, Width, Height (0.0-1.0)
    
    /// <summary>显示的图片路径（null表示使用当前图片的对应区域）</summary>
    public string ImagePath { get; set; }
    
    /// <summary>是否自动裁剪原图</summary>
    public bool AutoCrop { get; set; } = true;
    
    /// <summary>独立缩放比例</summary>
    public double ZoomRatio { get; set; } = 1.0;
    
    /// <summary>独立滚动偏移（用于关键帧记录）</summary>
    public double ScrollOffset { get; set; } = 0.0;
}

/// <summary>
/// 分割布局配置
/// </summary>
public class SplitLayoutConfig
{
    /// <summary>分割模式</summary>
    public SplitMode Mode { get; set; }
    
    /// <summary>各区域配置</summary>
    public List<SplitRegion> Regions { get; set; } = new List<SplitRegion>();
    
    /// <summary>是否显示分割线</summary>
    public bool ShowDividers { get; set; } = true;
    
    /// <summary>分割线颜色</summary>
    public string DividerColor { get; set; } = "#808080";
    
    /// <summary>分割线粗细</summary>
    public int DividerThickness { get; set; } = 2;
}
```

---

## 🏗️ 技术架构

### 核心组件结构

```
Core/
├── ImageSplitProcessor.cs        # 分割处理引擎（SkiaSharp + GPU）
└── SplitLayoutManager.cs         # 布局管理器

UI/
├── MainWindow.Split.cs           # 分割UI逻辑（partial class）
└── Controls/
    └── SplitRegionControl.cs     # 单个区域控件

Database/Models/
├── SplitConfig.cs                # 分割配置模型
└── MediaFile.cs (扩展)           # 添加分割模式字段

Managers/
└── SplitPlaybackManager.cs       # 分割模式播放管理
```

### 1. ImageSplitProcessor（分割引擎）

**职责**: 高性能图片分割和区域渲染

```csharp
public class ImageSplitProcessor
{
    private GPUContext _gpuContext;
    
    /// <summary>
    /// 将单张图片分割为多个区域
    /// </summary>
    public SKBitmap[] SplitImageAuto(SKBitmap source, SplitMode mode)
    {
        switch (mode)
        {
            case SplitMode.VerticalSplit:
                return SplitVertical(source);
            case SplitMode.HorizontalSplit:
                return SplitHorizontal(source);
            case SplitMode.Grid2x2:
                return SplitGrid2x2(source);
            default:
                return new[] { source };
        }
    }
    
    /// <summary>
    /// GPU加速裁剪
    /// </summary>
    private SKBitmap CropRegion(SKBitmap source, SKRectI cropRect)
    {
        // 使用GPU加速（如果可用）
        if (_gpuContext.IsGpuAvailable)
        {
            return CropWithGPU(source, cropRect);
        }
        else
        {
            return CropWithCPU(source, cropRect);
        }
    }
    
    /// <summary>
    /// 渲染分割布局到单个WriteableBitmap
    /// </summary>
    public WriteableBitmap RenderSplitLayout(SplitLayoutConfig config, 
        Dictionary<int, SKBitmap> regionImages, int canvasWidth, int canvasHeight)
    {
        // 创建最终输出画布
        var outputBitmap = new WriteableBitmap(canvasWidth, canvasHeight, 
            96, 96, PixelFormats.Bgra32, null);
        
        // 使用SkiaSharp渲染
        using var surface = SKSurface.Create(new SKImageInfo(canvasWidth, canvasHeight));
        var canvas = surface.Canvas;
        canvas.Clear(SKColors.Black);
        
        // 渲染各区域
        foreach (var region in config.Regions)
        {
            if (regionImages.TryGetValue(region.RegionId, out var image))
            {
                var destRect = CalculateDestRect(region.Bounds, canvasWidth, canvasHeight);
                canvas.DrawBitmap(image, destRect);
            }
        }
        
        // 绘制分割线
        if (config.ShowDividers)
        {
            DrawDividers(canvas, config, canvasWidth, canvasHeight);
        }
        
        // 转换为WriteableBitmap
        return ConvertToWriteableBitmap(surface);
    }
}
```

### 2. SplitLayoutManager（布局管理）

**职责**: 管理分割模式切换和区域配置

```csharp
public class SplitLayoutManager
{
    private SplitMode _currentMode = SplitMode.Single;
    private SplitLayoutConfig _currentConfig;
    
    /// <summary>
    /// 切换分割模式
    /// </summary>
    public void SetSplitMode(SplitMode mode)
    {
        _currentMode = mode;
        _currentConfig = GenerateDefaultConfig(mode);
    }
    
    /// <summary>
    /// 生成默认布局配置
    /// </summary>
    private SplitLayoutConfig GenerateDefaultConfig(SplitMode mode)
    {
        var config = new SplitLayoutConfig { Mode = mode };
        
        switch (mode)
        {
            case SplitMode.VerticalSplit:
                config.Regions.Add(new SplitRegion 
                { 
                    RegionId = 0, 
                    Bounds = new RectangleF(0, 0, 0.5f, 1) 
                });
                config.Regions.Add(new SplitRegion 
                { 
                    RegionId = 1, 
                    Bounds = new RectangleF(0.5f, 0, 0.5f, 1) 
                });
                break;
                
            case SplitMode.HorizontalSplit:
                config.Regions.Add(new SplitRegion 
                { 
                    RegionId = 0, 
                    Bounds = new RectangleF(0, 0, 1, 0.5f) 
                });
                config.Regions.Add(new SplitRegion 
                { 
                    RegionId = 1, 
                    Bounds = new RectangleF(0, 0.5f, 1, 0.5f) 
                });
                break;
                
            case SplitMode.Grid2x2:
                config.Regions.Add(new SplitRegion 
                { 
                    RegionId = 0, 
                    Bounds = new RectangleF(0, 0, 0.5f, 0.5f) 
                });
                config.Regions.Add(new SplitRegion 
                { 
                    RegionId = 1, 
                    Bounds = new RectangleF(0.5f, 0, 0.5f, 0.5f) 
                });
                config.Regions.Add(new SplitRegion 
                { 
                    RegionId = 2, 
                    Bounds = new RectangleF(0, 0.5f, 0.5f, 0.5f) 
                });
                config.Regions.Add(new SplitRegion 
                { 
                    RegionId = 3, 
                    Bounds = new RectangleF(0.5f, 0.5f, 0.5f, 0.5f) 
                });
                break;
        }
        
        return config;
    }
}
```

---

## 🎨 UI设计

### 1. 分割模式切换按钮

**位置**: 主菜单栏，"变色"按钮后

```xml
<!-- MainWindow.xaml 第527行后添加 -->
<Button x:Name="BtnSplitMode" Content="🔲 分割" 
        Style="{StaticResource MenuButtonStyle}"
        ToolTip="切换图片分割模式" 
        Click="BtnSplitMode_Click"/>
```

**点击行为**:
- 单击：循环切换模式（单区域 → 竖分割 → 横分割 → 四宫格 → 单区域）
- 右键：弹出完整菜单

### 2. 右键菜单

```csharp
private void ShowSplitModeMenu()
{
    var menu = new ContextMenu();
    
    AddMenuItem(menu, "● 单区域", SplitMode.Single, "🖼");
    AddMenuItem(menu, "▐▌ 竖分割", SplitMode.VerticalSplit, "▐▌");
    AddMenuItem(menu, "▬▬ 横分割", SplitMode.HorizontalSplit, "▬");
    AddMenuItem(menu, "田 四宫格", SplitMode.Grid2x2, "田");
    
    menu.AddSeparator();
    
    var loadImageItem = new MenuItem { Header = "📁 为区域加载图片..." };
    loadImageItem.Click += LoadImageToRegion_Click;
    menu.Items.Add(loadImageItem);
    
    var clearItem = new MenuItem { Header = "🗑 清除区域图片" };
    clearItem.Click += ClearRegionImages_Click;
    menu.Items.Add(clearItem);
    
    BtnSplitMode.ContextMenu = menu;
}
```

### 3. 分割区域显示

**方案A：Canvas叠加层（推荐）**

```xml
<!-- 在 ImageScrollViewer 内部添加 -->
<Canvas x:Name="SplitRegionsCanvas" 
        Background="Transparent"
        Visibility="Collapsed"
        Panel.ZIndex="5">
    <!-- 区域容器将动态添加 -->
</Canvas>
```

**方案B：Grid布局（更简单但灵活性较低）**

```xml
<Grid x:Name="SplitRegionsGrid" Visibility="Collapsed">
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
        <RowDefinition Height="*"/>
        <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    
    <Image x:Name="SplitImage0" Grid.Row="0" Grid.Column="0"/>
    <Image x:Name="SplitImage1" Grid.Row="0" Grid.Column="1"/>
    <Image x:Name="SplitImage2" Grid.Row="1" Grid.Column="0"/>
    <Image x:Name="SplitImage3" Grid.Row="1" Grid.Column="1"/>
</Grid>
```

---

## 💾 数据持久化

### 1. 数据库扩展

#### MediaFile 表扩展
```sql
ALTER TABLE MediaFiles ADD COLUMN SplitMode INTEGER DEFAULT 0;
ALTER TABLE MediaFiles ADD COLUMN SplitConfigJson TEXT;
```

```csharp
// Database/Models/MediaFile.cs
public partial class MediaFile
{
    /// <summary>分割模式</summary>
    public SplitMode SplitMode { get; set; } = SplitMode.Single;
    
    /// <summary>分割配置（JSON）</summary>
    public string SplitConfigJson { get; set; }
    
    /// <summary>获取分割配置对象</summary>
    [NotMapped]
    public SplitLayoutConfig SplitConfig
    {
        get => string.IsNullOrEmpty(SplitConfigJson) 
            ? null 
            : JsonSerializer.Deserialize<SplitLayoutConfig>(SplitConfigJson);
        set => SplitConfigJson = value == null 
            ? null 
            : JsonSerializer.Serialize(value);
    }
}
```

### 2. 全局设置

```csharp
// 在 Settings 表中添加
{
    Key = "DefaultSplitMode",
    Value = "0",  // SplitMode.Single
    Description = "默认分割模式"
}
```

---

## 🎬 播放系统集成

### 1. 关键帧记录扩展

```csharp
// Database/Models/Keyframe.cs
public partial class Keyframe
{
    /// <summary>分割模式</summary>
    public SplitMode SplitMode { get; set; }
    
    /// <summary>各区域配置JSON</summary>
    public string SplitRegionsJson { get; set; }
}
```

### 2. 播放逻辑调整

```csharp
// Services/KeyframePlaybackService.cs
private async Task JumpToKeyframe(Keyframe keyframe)
{
    // 恢复分割模式
    if (keyframe.SplitMode != SplitMode.Single)
    {
        _mainWindow.SetSplitMode(keyframe.SplitMode);
        // 恢复各区域图片
        RestoreSplitRegions(keyframe.SplitRegionsJson);
    }
    
    // 原有滚动逻辑...
}
```

### 3. 原图模式适配

**问题**: 原图模式下分割功能如何表现？

**方案**: 
- 原图模式时，分割模式**暂时隐藏**
- 退出原图模式后**恢复**之前的分割状态
- 记录在内存中，不影响数据库状态

---

## 🎯 实施计划

### Phase 1: 核心引擎（2-3天）
- [ ] 创建 `ImageSplitProcessor.cs`
- [ ] 实现基础分割算法（单图自动分割）
- [ ] 实现GPU加速裁剪
- [ ] 测试性能（目标：<50ms完成分割）

### Phase 2: UI交互（1-2天）
- [ ] 添加分割模式切换按钮
- [ ] 实现右键菜单
- [ ] 创建分割区域显示容器
- [ ] 实现模式切换动画效果

### Phase 3: 多图加载（1-2天）
- [ ] 实现"为区域加载图片"功能
- [ ] 添加拖拽支持（拖图片到指定区域）
- [ ] 实现区域图片预览

### Phase 4: 数据持久化（1天）
- [ ] 扩展数据库表结构
- [ ] 实现配置的保存和加载
- [ ] 迁移现有数据

### Phase 5: 播放集成（2天）
- [ ] 关键帧录制时保存分割状态
- [ ] 播放时恢复分割状态
- [ ] 投影窗口同步分割显示

### Phase 6: 测试优化（1-2天）
- [ ] 性能测试（分割速度、渲染帧率）
- [ ] 边界情况测试（超大图、特殊比例）
- [ ] 用户体验优化
- [ ] 文档完善

**总预计时间**: 8-12 天

---

## 🔍 性能目标

| 指标 | 目标值 | 测试场景 |
|------|--------|---------|
| 分割响应时间 | < 100ms | 4K图片 → 四宫格 |
| 渲染帧率 | ≥ 60 FPS | 分割模式下滚动 |
| 内存增长 | < 50% | 对比单图模式 |
| GPU占用 | < 30% | 四宫格播放 |

---

## ⚠️ 技术风险和应对

### 风险1: 性能问题
**场景**: 四宫格模式下，4张4K图片同时渲染可能卡顿

**应对**:
- 使用LRU缓存，提前预渲染
- 降低非活动区域的渲染质量
- 支持"单区域高清，其他区域预览"模式

### 风险2: 滚动冲突
**场景**: 各区域独立滚动时，同步问题

**应对**:
- Phase 1只实现"整体滚动"
- Phase 2再考虑"区域独立滚动"（可选）

### 风险3: 投影同步
**场景**: 投影窗口需要同步分割状态

**应对**:
- 复用现有投影机制
- 分割配置通过事件传递

---

## 📝 待定问题

1. **缩放行为**: 分割模式下，缩放是整体缩放还是各区域独立？
   - **建议**: Phase 1实现整体缩放，Phase 2支持独立缩放

2. **变色效果**: 分割模式下，变色是统一应用还是各区域独立？
   - **建议**: 统一应用，简化逻辑

3. **关键帧**: 是否为每个区域独立记录滚动位置？
   - **建议**: Phase 1不支持，Phase 2可选

---

## 🎉 成功标准

- ✅ 用户可以轻松切换4种分割模式
- ✅ 支持单图自动分割和多图独立加载
- ✅ 分割状态可以保存和恢复
- ✅ 播放和录制功能完全兼容
- ✅ 性能满足目标指标
- ✅ 无明显Bug和卡顿

---

## 📚 参考资料

- [SkiaSharp文档 - Canvas绘制](https://docs.microsoft.com/en-us/xamarin/xamarin-forms/user-interface/graphics/skiasharp/)
- [WPF性能优化指南](https://docs.microsoft.com/en-us/dotnet/desktop/wpf/advanced/optimizing-performance-overview)
- Canvas Python版本相关代码（参考实现思路）

---

*文档版本: V1.0*  
*最后更新: 2025-10-19*

